venv/
__pycache__/
*.pyc
.DS_Store

# The actual 100k-candidate dataset is large (~465MB uncompressed) and is
# provided by the hackathon organizers, not something we should commit --
# keep the repo lean and avoid redistributing their dataset.
data/candidates.jsonl
data/candidates.jsonl.gz

# Generated output, not source -- regenerated by running rank.py
output/*.csv

# Keep job_description.md and any small reference docs (they're useful
# context for anyone reading the repo and are not large files).
